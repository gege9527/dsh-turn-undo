
# 文件撤销功能完整性分析报告

## 核心需求
确保文件撤销功能能完整回退到目标状态，不能有任何遗漏。

## 当前实现的问题

### 1. 快照捕获阶段的问题

#### Bug 2: copyIntoObjects 流式回退不验证副本
- **位置**: index.js:358-375
- **问题**: copyFileSync 失败后进入流式回退，但 finish 事件不保证内容正确
- **影响**: 对象文件可能包含错误内容，但 manifest 中的 hash 指向它
- **修复**: 在 copyIntoObjects 中验证写入后的文件 hash

#### Bug 3: 文件权限变更不被检测
- **位置**: index.js:232-233
- **问题**: unchanged 检测只比较 size 和 mtime，不比较 mode
- **影响**: 文件权限变更永久丢失
- **修复**: unchanged 检测应包含 mode 比较

### 2. 文件恢复阶段的问题

#### Bug 4: restore 静默跳过缺失的对象
- **位置**: index.js:443-453
- **问题**: 当对象文件不存在时，continue 静默跳过
- **影响**: 用户不知道哪些文件没有恢复
- **修复**: 记录跳过的文件列表，并在返回结果中告知用户

#### Bug 5: restore 部分失败导致工作区不一致
- **位置**: index.js:417-469
- **问题**: 每个文件有 try/catch，但不会中断循环
- **影响**: 工作区被置于不一致状态（部分文件目标状态，部分旧状态）
- **修复**: 记录失败的步骤和文件，返回部分成功状态

### 3. 预览阶段的问题

#### Bug 6: 安全性快照的 turn 编号排序可能导致预览异常
- **位置**: index.js:928-931
- **问题**: safety snapshot 编号可能与后续 turn start 重叠
- **影响**: preview 显示不准确的 changes
- **修复**: 使用更大的偏移量（如 lastTurn + 0.999）

## 完整性保证方案

### 1. 快照捕获阶段
- 验证 copyIntoObjects 写入的文件 hash
- 记录无法写入的文件并返回错误
- unchanged 检测包含 mode 比较

### 2. 文件恢复阶段
- 记录每个文件的恢复状态
- 返回部分成功状态（包含成功/失败列表）
- 在极端情况下保持工作区一致性

### 3. 预览阶段
- 修正 safety snapshot 编号
- 确保 preview 显示准确的 changes

## 修复优先级
1. Bug 2: copyIntoObjects 流式回退验证（最严重）
2. Bug 4: restore 静默跳过对象（影响用户体验）
3. Bug 5: restore 部分失败导致工作区不一致（可能导致数据丢失）
4. Bug 3: 文件权限变更不被检测（影响完整性）
5. Bug 6: 安全性快照的 turn 编号排序（影响预览准确性）
